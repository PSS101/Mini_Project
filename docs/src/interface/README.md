# Interface folder

## client.c
- main(): starts the simple client entry point.

## dev_cli.c
- start_dev_cli(): launches the developer shell for manual testing.

## Workflow
The interface layer is the front door for user and developer interaction.
