# Core utils folder

## hash.c
- hash(): creates a deterministic hash for internal storage and lookup use.

## Workflow
The utility layer provides shared helpers that simplify implementation in the core and data-type modules.
