# List datatype folder

## list.c
- createList(): allocates a list object.
- lpush(), rpush(): add values to either side.
- lpop(), rpop(): remove values from either side.
- freeList(): releases list memory.

## Workflow
List operations are used for commands that need ordered, appendable collections.
